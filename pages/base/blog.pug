mixin blog(post)
  div.blog
    h2
      a(href=post.link)=post.title
    small.datemark
      | Created 
      = post.created
      if post.updated 
        | , updated 
        = post.updated
    p=post.teaser